<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Signature permission declaration makes the public API contract explicit.
         A normal Play-distributed build is not granted this permission; the probe
         records whether carrier authorization or Android's resolution path works. -->
    <uses-permission android:name="android.permission.WRITE_EMBEDDED_SUBSCRIPTIONS" />

    <application
        android:label="eSIM feasibility probe"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:name=".ProbeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
